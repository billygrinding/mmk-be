syntax = "proto3";
package proto;
option go_package="/pb";

// HealthCheckService
service HealthCheckService {
  rpc HealthCheck(Request) returns (Response);
}

message Request {
  string value = 1;
}

message Response {
  Status value = 1;
}

// Status
enum Status {
  OK = 0;
  FAIL = 1;
}